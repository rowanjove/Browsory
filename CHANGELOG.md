# 更新日志 (Changelog)

本项目遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/) 规范。

## [0.1.2] - 2026-09-21

### 修复与产品完整性
- 启用 PIN 后保持当前会话解锁，直到用户保存恢复密钥；PIN 文案改为「只锁界面，不加密数据库」。
- 浏览器运行中的手动同步改为只读快照路径，不再默认结束浏览器进程。
- Pro 功能在后端按授权拒绝（语义检索、离线归档、WebDAV）；免费能力保持可用。
- 关于页与侧栏显示真实版本，并支持对照 GitHub Releases 检查更新。
- 关闭窗口询问退出或最小化到托盘；便携版在存在 `portable.txt` 时把数据写到程序旁。
- 导出最多 10 万条、向量检索最多扫描 4000 条候选，避免一次装入全部记录。
- 离线归档重复保存时返回正确行 id。

## [0.1.1] - 2026-09-20

### 修复内容
- 修复浏览器历史归档替换、删除与路径校验，避免残留内容和目录穿越。
- 修复 WebDAV 配置读取、凭据存储和服务端校验，未实现的双向同步改为安全拒绝。
- 修复 AI 远端请求的端点校验与隐私脱敏，限制不安全的远程连接。
- 修复数据库迁移的数据保留与备份命名，并明确迁移不等同于 SQLite 加密。
- 修复离线阅读索引、设置页隐私说明和同步状态文案，避免将本地索引误报为远程快照或端到端同步。
- 补充归档、同步、AI 端点和安全配置回归测试，并完善 CI 发布前检查。

## [0.1.0] - 2026-09-14

### 核心功能
- **多浏览器自动扫描与聚合**：自动检测系统内安装的 Chromium 内核浏览器（Chrome、Edge、Brave、Vivaldi 等）及其多个用户配置文件（Profile），通过只读快照机制读取历史数据，避免与运行中的浏览器发生文件锁冲突。
- **毫秒级全文检索 (FTS5)**：集成 SQLite FTS5 全文检索引擎，支持即时模糊搜索标题与网址，提供多维度交叉筛选（日期范围、域名、浏览器源、收藏状态）。
- **足迹统计分析看板**：提供访问趋势统计、24 小时生活节奏热力图、高频访问域名排行以及周期访问对比。
- **本地优先与隐私防护**：所有数据全生命周期存储在本地 SQLite 数据库中，不设云端回传；支持设置应用程序 PIN 码安全锁及输错指数退避防护策略。
- **外部数据导入与归档导出**：支持导入 Google Takeout 导出的历史归档 JSON 文件，支持外部历史 SQLite 数据库拖拽导入，支持导出为 CSV / Excel 格式。
- **AI 智能足迹助手**：支持接入本地部署的 Ollama 或兼容 OpenAI 协议的模型接口，基于检索到的本地历史上下文实现问答回忆与知识总结。
